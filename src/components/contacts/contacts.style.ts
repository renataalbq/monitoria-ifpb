import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    row: {
        flexDirection: 'row',
        marginHorizontal: 30,
        backgroundColor: "#ebebeb",
        marginTop: 10,
        padding: 10,
    },
    textContact: {
        fontSize: 16,
        marginLeft: 10,

    }
});
