import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    input: {
        backgroundColor: '#ececec',
        width: 280,
        borderRadius: 10,
        height: 50,
        paddingLeft: 15,
    },
    caption: {
        color: "#ff0000",
        fontSize: 12,
        textAlign: "center",
        marginTop: 8,
    }
});
