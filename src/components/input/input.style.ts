import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    input: {
        borderWidth: 1,
        borderColor: "#777777",
        width: 320,
        borderRadius: 14,
        height: 50,
        paddingLeft: 15,
    },
    caption: {
        color: "#ff0000",
        fontSize: 12,
        textAlign: "center",
        marginTop: 8,
    }
});
